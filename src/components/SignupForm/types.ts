export type File = {
  path: string;
};

export type ValidationsValues = {
  title: string;
  speaker: string;
  url: string;
  spots: string;
  startDate: string;
  startDateTime: string;
  image: any;
  endDate: string;
  endDateTime: string;
  duration: string;
  pickedLevel: string;
  pickedTags: string[];
};
